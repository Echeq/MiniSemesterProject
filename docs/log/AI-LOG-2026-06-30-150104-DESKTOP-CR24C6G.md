# AI Interaction Log — 2026-06-30

| Field | Value |
|---|---|
| PC Name | DESKTOP-CR24C6G |
| Date | 2026-06-30 |
| Time | 150104 |
| Branch | pre-deploy |
| Tool | OpenCode |

## Prompt

> @ai-orchestrator haz que los usuarios tengan el drag&drops denuevo y no click&chose en modo monitor y en modo celular que tengan solo el click&choose no drag&drop.

## AI Response

Re-enabled drag-and-drop for all users on desktop; mobile now uses tap-menu only (click&choose). Changes to `Board.jsx`, `Column.jsx`, `TaskCard.jsx`.

**Behavior matrix after fix:**

| | Desktop | Mobile |
|---|---|---|
| Drag & drop | All users | None |
| Tap card | Admin: opens TaskModal. Members: nothing | All users: opens move menu |
| + button (create) | Admin only | Admin only |
| Edit button | Admin only (in TaskModal) | Admin only (in mobile menu) |
| Move task | Via drag (all users) | Via tap menu (all users) |

## Files Affected

- `frontend/src/components/Board.jsx` — removed isAdmin gates from drag handlers and handleTaskTap; removed canDrag prop from Column renderings; removed isAdmin gate from mobile move buttons
- `frontend/src/components/Column.jsx` — added `canDrag` prop threaded to TaskCard
- `frontend/src/components/TaskCard.jsx` — added `canDrag` prop to useSortable disabled logic

## Code Before

```diff
  const handleDragStart = useCallback(({ active }) => {
-   if (!isAdmin) return
    setActiveTask(tasksRef.current.find((t) => t.id === active.id) ?? null)
- }, [isAdmin])
+ }, [])

  const handleDragEnd = useCallback(({ active, over }) => {
    setActiveTask(null)
-   if (!over || !isAdmin) return
+   if (!over) return
    ...
    updateTask(task.id, { status: targetStatus, position }).catch(() => {})
- }, [updateTask, isAdmin])
+ }, [updateTask])

  const handleTaskTap = useCallback((task) => {
    if (isMobile) {
      setMobileMenuTask(task)
    } else {
-     if (!isAdmin) return
      onTaskClick(task)
    }
- }, [isMobile, onTaskClick, isAdmin])
+ }, [isMobile, onTaskClick])

- {isAdmin && STATUSES.filter((s) => s !== mobileMenuTask.status).map((s) => (
+ {STATUSES.filter((s) => s !== mobileMenuTask.status).map((s) => (

- canDrag={isAdmin}   // removed from both Column renderings
```

```diff
// Column.jsx
- function Column({ status, tasks, onTaskClick, onAddTask, editors }) {
+ function Column({ status, tasks, onTaskClick, onAddTask, editors, canDrag = true }) {
    ...
-   <TaskCard ... editors={editors} />
+   <TaskCard ... editors={editors} canDrag={canDrag} />
```

```diff
// TaskCard.jsx
- function TaskCard({ task, onClick, overlay = false, editors, isMobile }) {
+ function TaskCard({ task, onClick, overlay = false, editors, isMobile, canDrag = true }) {
    ...
-   disabled: overlay || isMobile,
+   disabled: overlay || isMobile || !canDrag,
```

## Code After

### `frontend/src/components/Board.jsx` (key sections)

```javascript
const handleDragStart = useCallback(({ active }) => {
    setActiveTask(tasksRef.current.find((t) => t.id === active.id) ?? null)
  }, [])

const handleDragEnd = useCallback(({ active, over }) => {
    setActiveTask(null)
    if (!over) return
    // ... position logic ...
    updateTask(task.id, { status: targetStatus, position }).catch(() => {})
  }, [updateTask])

const handleTaskTap = useCallback((task) => {
    if (isMobile) {
      setMobileMenuTask(task)
    } else {
      onTaskClick(task)
    }
  }, [isMobile, onTaskClick])
```

### `frontend/src/components/Column.jsx`

```javascript
function Column({ status, tasks, onTaskClick, onAddTask, editors, canDrag = true }) {
  // ...
  {tasks.map((task) => (
    <TaskCard key={task.id} task={task} onClick={onTaskClick} editors={editors} canDrag={canDrag} />
  ))}
}
```

### `frontend/src/components/TaskCard.jsx`

```javascript
function TaskCard({ task, onClick, overlay = false, editors, isMobile, canDrag = true }) {
  const { attributes, listeners, setNodeRef, transform, transition, isDragging } = useSortable({
    id: task.id,
    disabled: overlay || isMobile || !canDrag,
  })
}
```
