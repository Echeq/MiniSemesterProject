# AI Interaction Log — 2026-07-01

| Field | Value |
|---|---|
| PC Name | DESKTOP-CR24C6G |
| Date | 2026-07-01 |
| Time | 222452 |
| Branch | main |
| Tool | OpenCode |

## Prompt

User requested restricting member permissions:
- Members should ONLY be able to drag & drop (desktop) and use click & choose (mobile)
- Members should NOT be able to click project title, edit projects, or open task modals
- On mobile, members click a task and choose which column to move it to (no Edit/View)
- Admins keep full control as before
- Adapt for both desktop and mobile with translations if needed

## AI Response

Explored Board.jsx, Topbar.jsx, and TaskCard.jsx to understand click and drag flow. Identified 3 changes:

1. **Board.jsx — desktop columns** (line 248): Changed `onTaskClick={onTaskClick}` to `onTaskClick={isAdmin ? onTaskClick : undefined}` — members clicking task cards on desktop do nothing (drag & drop only).

2. **Board.jsx — mobile bottom sheet** (lines 274-282): Wrapped the Edit/View button in `{isAdmin && (...)}` — members on mobile see only "Move to X" buttons + "Cancel" when tapping a task.

3. **Board.jsx — fixed broken translation** (line 298): Changed `t('board.moveTo')` to `t('task.moveTo')` — `board.moveTo` didn't exist in any locale, was showing raw key string.

4. **Topbar.jsx — project title** (lines 66-72): Changed `project` to `project && isAdmin` for click handlers and ProjectMenu rendering — members can no longer click project title.

All 23 tests passed across 5 test files.

## Files Affected

- `frontend/src/components/Board.jsx` — Desktop click disabled for members, mobile bottom sheet restricted, fixed translation key
- `frontend/src/components/Topbar.jsx` — Project title clickable only for admins

## Code Before

```diff
--- Board.jsx (before)
+++ Board.jsx (after)
@@ -245,7 +245,7 @@
                     <Column
                       status={status}
                       tasks={byStatus[status]}
-                      onTaskClick={onTaskClick}
+                      onTaskClick={isAdmin ? onTaskClick : undefined}
                       onAddTask={onAddTask}
                       editors={editors}
                     />
@@ -271,14 +271,16 @@
           >
             <div className="mx-auto mb-3 h-1 w-10 rounded-full bg-[var(--border)]" />
             <p className="mb-3 text-sm font-semibold truncate">{mobileMenuTask.title}</p>
-            <div className="flex flex-col gap-2">
+            {isAdmin && (
               <button
                 type="button"
                 onClick={() => { onTaskClick(mobileMenuTask); setMobileMenuTask(null) }}
                 className="btn btn-primary w-full justify-center"
               >
-                {isAdmin ? t('task.edit') : t('task.view')}
+                {t('task.edit')}
               </button>
+            )}
+            <div className={`flex flex-col gap-2 ${isAdmin ? '' : 'mt-0'}`}>
               {STATUSES.filter((s) => s !== mobileMenuTask.status).map((s) => (
                 <button
                   key={s}
@@ -293,7 +295,7 @@
                   }}
                   className="btn btn-default w-full justify-center"
                 >
-                  {t('board.moveTo')} {t(STATUS_LABEL_KEYS[s])}
+                  {t('task.moveTo')} {t(STATUS_LABEL_KEYS[s])}
                 </button>
               ))}
               <button

--- Topbar.jsx (before)
+++ Topbar.jsx (after)
@@ -63,13 +63,13 @@
     <header>
       <div className="flex min-w-0 items-center gap-2.5">
         <h1
-          className={`... ${project ? 'cursor-pointer...' : ''}`}
-          onClick={project ? () => setShowMenu((s) => !s) : undefined}
-          role={project ? 'button' : undefined}
-          tabIndex={project ? 0 : undefined}
-          onKeyDown={project ? (e) => e.key === 'Enter' && setShowMenu((s) => !s) : undefined}
+          className={`... ${project && isAdmin ? 'cursor-pointer...' : ''}`}
+          onClick={project && isAdmin ? () => setShowMenu((s) => !s) : undefined}
+          role={project && isAdmin ? 'button' : undefined}
+          tabIndex={project && isAdmin ? 0 : undefined}
+          onKeyDown={project && isAdmin ? (e) => e.key === 'Enter' && setShowMenu((s) => !s) : undefined}
         >{title}</h1>
-        {showMenu && project && (
+        {showMenu && project && isAdmin && (
           <ProjectMenu ... />
         )}
```

## Code After

See git diff above. Key structure:
- Board.jsx: Desktop `onTaskClick` conditionally undefined for members; mobile Edit button wrapped in `isAdmin` check; broken `board.moveTo` key replaced with `task.moveTo`
- Topbar.jsx: All title click handlers gated on `project && isAdmin` instead of just `project`

## Translation note

No new translation keys needed. Existing `task.moveTo`, `task.edit`, `task.cancel` keys reused. Fixed one bug: `board.moveTo` → `task.moveTo` (key didn't exist, was showing raw string).
```
