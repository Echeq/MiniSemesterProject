# 📋 第三组 — 陈昌发 — 小组周会会议纪要（第二周）

---

## 一、会议基本信息

| 项目 | 内容 |
|------|------|
| **项目名称** | 面向小微团队的可视化任务调度与管理系统 |
| **报告起始日期** | 2026年6月23日（周一） |
| **预计结束日期** | 2026年6月28日（周六） |
| **会议地点** | 工程实践创新中心A座 201室 |

### 参会人员

| 角色 | 姓名 | GitHub | 学号 |
|:----|:-----|:-------|:-----|
| 👑 **组长 / 全栈质量保证** | 陈昌发 | [@Echeq](https://github.com/Echeq) | I202320150 |
| 🎨 **前端开发负责人** | 孔刚 | [@goanarbolkong](https://github.com/goanarbolkong) | I202420016 |
| ⚙️ **后端开发负责人** | 周奕龙 | [@MemerZxZ](https://github.com/MemerZxZ) | I202320080 |

---

## 二、上周遗留问题排查

> 📖 参考：[第一周会议纪要](第三组%20-%20陈昌发%20-%20小组周会会议纪要（第一周）.md)
>
> 本周启动时对第一周标记的问题逐项排查，确认真实状态：

### 🔴 关键问题 — 排查结果

| 问题 | 排查结论 | 本周计划 |
|:----|:----|:---------|
| 缺少 `project_members` 关联表 | ⚠️ 第一周审计遗漏 — 迁移 `20260618120000` 已创建该表（含 RLS + trigger），实际已存在 | 无需额外工作 |
| 优先级系统未实现（P0-P3） | ❌ 确认缺失 — `tasks` 表无 `priority` 列 | 周奕龙 Day 1-2：DB migration / 孔刚 Day 3：前端选择器 |
| 标签系统未实现 | ❌ 确认缺失 — 无 `labels` + `task_labels` 表 | 周奕龙 Day 1-2：DB migration / 孔刚 Day 4：标签管理器 UI |
| 任务依赖系统未实现 | ❌ 确认缺失 — 无 `task_dependencies` 表 | 周奕龙 Day 4：DB migration + RPC |
| task.md 大量 checkbox 未更新 | ⚠️ 确认 — 138 checkboxes 与审计结果不一致 | 陈昌发 Day 1：同步更新（已完成） |

### 🟡 次要问题 — 排查结果

| 问题 | 排查结论 | 本周计划 |
|:----|:----|:---------|
| RLS 未按 `project_id` 过滤 | ❌ 确认 — 成员可看到所有项目任务 | 周奕龙 Day 3：修改 tasks RLS 策略 |
| 无项目设置页面 | ❌ 确认 — 无编辑项目名称/描述的 UI | 孔刚 Day 1-2：ProjectSettings modal |
| 无项目颜色/图标选择器 | ❌ 确认 — DB 列 (`color`, `icon`) 已存在但无前端 UI | 孔刚 Day 1-2：color picker + icon selector |
| 无提醒/通知系统 | ⏳ 本周不处理 | 第三周 |
| 无 Gantt/3D/ListView | ⏳ 本周不处理 | 第三周 |
| 无 CI/CD 流水线 | ⏳ 本周不处理 | 配合 Vercel 部署时配置 |

### 🟢 技术债务 — 状态

| 项目 | 状态 |
|:-----|:----:|
| `socket.io-client` 未使用 | ⚠️ 待确认是否已从 package.json 移除 |
| `xlsx` 未使用 | ⚠️ 待确认是否已从 package.json 移除 |
| `backend/` 目录遗留 | ⏳ 低优先级 |
| `InviteDialog.jsx` 未使用 | ⏳ 旧列名无法在当前 schema 工作 |
| `AccountModal.jsx` 未使用 | ⏳ 已被 `ProfileModal.jsx` 替代 |
| `ProfileSettings.jsx` 未使用 | ⏳ 已被 `ProfileModal.jsx` 替代 |

---

## 三、本周需求规格与目标预测

> 📖 需求规格文档：[📄 task.md](task.md) · 需求追溯矩阵：[📊 requirements-matrix.md](requirements-matrix.md)
>
> 🎯 起始点：83/174 项完成（48%）→ 本周预测目标：~118/174 项完成（68%）

下表列出 `task.md` 中 checkbox 状态与本周开发计划对照：

| 模块 | 上周完成 | 本周新增 | 本周结束目标 | 主要工作 |
|:----|:-------:|:-------:|:----------:|:---------|
| **M1 — 用户与权限管理** | 15 (100%) | 0 | 15 (100%) | ✅ 已完成 |
| **M1 — 个人设置** | 12 (100%) | 0 | 12 (100%) | ✅ 已完成 |
| **M2 — 项目管理** | 4/9 (44%) | +4 | 8/9 (89%) | project settings + color/icon picker + updateProject API |
| **M3-P1 — 任务CRUD** | 12/14 (86%) | +1 | 13/14 (93%) | RLS by project_id |
| **M3-P2 — 拖拽与看板** | 12/13 (92%) | 0 | 12/13 (92%) | — |
| **M3-P3 — 优先级/标签/截止日期** | 5/16 (31%) | +8 | 13/16 (81%) | priority字段 + labels表 + 前端选择器 |
| **M3-P4 — 任务分配与依赖** | 5/13 (38%) | +5 | 10/13 (77%) | task_dependencies表 + checkBlockedTasks RPC + UI |
| **M4-P1 — 看板可视化** | 5/7 (71%) | +2 | 7/7 (100%) | 快速筛选 + 加载骨架 |
| **M4-P2 — Gantt 图** | 0/7 (0%) | 0 | 0/7 (0%) | ⏳ 下周 |
| **M4-P3 — 3D 球体可视化** | 0/10 (0%) | 0 | 0/10 (0%) | ⏳ 下周 |
| **M4-P4 — 列表视图与筛选** | 0/9 (0%) | 0 | 0/9 (0%) | ⏳ 下周 |
| **M5 — 实时协作** | 9/9 (100%) | 0 | 9/9 (100%) | ✅ 已完成（task.md 已同步） |
| **M6 — 数据导出** | 0/7 (0%) | 0 | 0/7 (0%) | ⏳ 下周 |
| **M7 — 系统管理** | 2/9 (22%) | +3 | 5/9 (55%) | system_logs表 + logActivity RPC + backup RPC |
| **Final — 收尾与部署** | 4/20 (20%) | 0 | 4/20 (20%) | task.md 同步、requirements-matrix.md、AGENTS.md 优化 |
| **总计** | **83/174 (48%)** | **+23** | **~118/174 (68%)** | |

> 📊 新增文件：`docs/requirements-matrix.md` — 15 模块完整追溯矩阵，含每个任务→源文件映射 + 技术债务清单

---

## 四、本周工作计划（6天：6月23日 → 6月28日）

### 任务分配

#### 🔙 后端 — 周奕龙 ([@MemerZxZ](https://github.com/MemerZxZ))

| Day | 任务 | 产出物 |
|:---:|:-----|:-------|
| **D1-2** | `priority` enum (P0-P3) + `labels` + `task_labels` 单次迁移（含 RLS） | 1 个 migration SQL |
| **D3** | RLS 按 `project_id` 过滤 tasks + 修改现有 policies | SQL migration |
| **D4** | `task_dependencies` 表 + `checkBlockedTasks()` RPC | migration + RPC |
| **D5** | `system_logs` 表 + `logActivity()` RPC | migration + RPC |
| **D6** | `exportAllData()` JSON dump RPC + `getFilteredTasks()` RPC（参数：status, priority, assignee, date range, labels） | 2 个 RPCs |

#### 🎨 前端 — 孔刚 ([@goanarbolkong](https://github.com/goanarbolkong))

| Day | 任务 | 产出物 |
|:---:|:-----|:-------|
| **D1-2** | Project settings modal（编辑名称/描述）+ Color picker（预设调色板）+ Icon picker | `ProjectSettingsModal.jsx` |
| **D3** | Priority 选择器（P0-P3 带颜色 badge）+ TaskCard 上显示 priority badge | 修改 `TaskModal.jsx` + `TaskCard.jsx` |
| **D4** | Labels CRUD modal（per project）+ TaskModal 多选标签 + TaskCard 标签 chips | `LabelManager.jsx`，修改 `TaskModal.jsx` + `TaskCard.jsx` |
| **D5** | Task dependency 选择器（"blocked by"）+ TaskCard 阻塞 badge + 阻塞任务完成拦截 | 修改 `TaskModal.jsx` + `TaskCard.jsx` |
| **D6** | Loading skeletons（Board + Sidebar）+ Quick column filters（显示/隐藏 Todo/Doing/Done） | `<Skeleton />` 组件 + Board 修改 |

#### 👑 QA/PM — 陈昌发 ([@Echeq](https://github.com/Echeq))

| Day | 任务 | 产出物 |
|:---:|:-----|:-------|
| **D1** | 同步 task.md 138 checkboxes 与审计结果 | task.md 更新 |
| **D2** | 创建 requirements-matrix.md 追溯矩阵 + 优化 AGENTS.md | requirements-matrix.md + AGENTS.md |
| **D3-4** | 测试新组件：project settings, priority, labels | `tests/components/` |
| **D5** | 集成测试新 RPCs + 手动验证 RLS policies | `tests/api.test.js` 补充 |
| **D6** | 周中审计：统计完成率，更新 task.md，标记偏差 | 审计报告 |

---

## 五、Day 1 启动工作（文档准备）

> 📅 2026年6月23日 — 本周启动时完成的基础文档工作。以下为产出物说明，供后续开发参考。

### 5.1 📄 AGENTS.md 优化

> 文件：[AGENTS.md](../../AGENTS.md)

| 变更 | 说明 |
|:-----|:-----|
| 删除 hooks 概述表 | AI agent 可自行读取 hooks 文件，无需维护列表 |
| 删除 migration 详情表 | 文件列表可通过 glob 获取，保留总数即可 |
| 删除 "89 tests" 具体数字 | 测试数量频繁变化，保留 `api.test.js` 排除规则即可 |
| 删除常规约定（.env gitignore等） | AI agent 无需被告知 |
| 新增：无 lint/typecheck 脚本 | agent 不会浪费时间尝试 `npm run lint` |
| 新增：无 CI 流水线 | agent 不会搜索 `.github/workflows/` |
| 新增：root package.json 仅为依赖桩 | agent 不会在 root 执行 `npm install` |
| 新增：socket.io-client 未使用 | agent 不会尝试集成 Socket.IO |
| 新增：`npm run preview` 命令 | 补充遗漏的 Vite preview |
| 长度 | 106 → ~55 行 |

### 5.2 📄 task.md 同步

> 文件：[task.md](task.md)

| 模块 | 同步内容 |
|:-----|:-----|
| **M2** | `project_members` → [x]（migration 已存在）/ color picker → [ ]（DB 列存在，UI缺失）/ updateProject API → 拆分为独立 [ ] / acceptance criterion "编辑项目详情" → 修正为 [ ] |
| **M5** | 全部 checkbox 已由第一周同步至 [x]（审计 100% 完成但 task.md 未勾选） |
| **M7** | Admin Panel → [x] / "Only admins access admin panel" → [x] |
| **Final** | Unit tests → [x]（89 tests）/ Dark mode → [x] / README → [x] / API docs → [x] |

### 5.3 📄 requirements-matrix.md 创建

> 新文件：[requirements-matrix.md](requirements-matrix.md)

- 15 模块完整追溯矩阵：每个任务 → 实现文件路径 + 状态（✅/🔄/❌）
- 技术债务清单：`InviteDialog.jsx`、`AccountModal.jsx`、`ProfileSettings.jsx` 未使用
- dead code 标记：`backend/`、`frontend/src/composables/`、`frontend/src/assets/`

---

## 六、模块开发详情

> ⚠️ 以下为本周目标，各模块完成后由对应负责人补充实现细节和代码引用

### 6.1 🔄 M2 — 项目管理（目标 44% → 89%）

**Day 1-2 前端（孔刚）：**
- [ ] Project settings modal（编辑名称、描述）
- [ ] Color picker（预设调色板：indigo, emerald, amber, rose, violet, cyan）
- [ ] Icon picker（预设图标集）
- [ ] 升级创建项目为独立 modal（含描述、颜色、图标）

**Day 1 后端（周奕龙）：**
- [ ] `updateProject` API（修改 `useProjects.js`）
- [ ] 列级授权：authenticated 可更新 (name, description, color, icon)

📄 参考实现：`useProjects.js` · `Sidebar.jsx` · `migrations/20260618120000_project_members_and_colors.sql`

---

### 6.2 🔄 M3-P3 — 优先级/标签（目标 31% → 81%）

**Day 1-2 后端（周奕龙）：**
- [ ] `tasks` 表新增 `priority` 列（text, check constraint: P0/P1/P2/P3 + null）
- [ ] `labels` 表（id uuid PK, name text, color text, project_id uuid FK, created_at）
- [ ] `task_labels` junction 表（task_id FK, label_id FK, unique(task_id, label_id)）
- [ ] RLS policies for labels + task_labels

**Day 3 前端（孔刚）：**
- [ ] Priority 选择器 UI（P0🔴 P1🟠 P2🟡 P3🟢 + none）
- [ ] TaskCard priority badge（彩色圆点 + 文字）
- [ ] TaskModal priority select

**Day 4 前端（孔刚）：**
- [ ] Label CRUD modal（创建/编辑/删除标签 per project）
- [ ] TaskModal 多选标签（checkbox list or multi-select dropdown）
- [ ] TaskCard 标签 chips（彩色背景 + 白色文字）

📄 新文件预期：`LabelManager.jsx`
📄 修改文件预期：`TaskModal.jsx` · `TaskCard.jsx`

---

### 6.3 🔄 M3-P4 — 任务依赖（目标 38% → 77%）

**Day 4 后端（周奕龙）：**
- [ ] `task_dependencies` 表（id uuid PK, task_id FK→tasks, depends_on_id FK→tasks, unique(task_id, depends_on_id), created_at）
- [ ] RPC: `checkBlockedTasks(task_id uuid)` → returns array of incomplete blocking tasks
- [ ] RPC: `addTaskDependency(task_id uuid, depends_on_id uuid)`
- [ ] Trigger 防止循环依赖

**Day 5 前端（孔刚）：**
- [ ] TaskModal 依赖选择器（"Blocked by" multi-select，显示当前项目其他任务）
- [ ] TaskCard 阻塞 badge（⛔ icon + "Blocked by X tasks" 文字）
- [ ] 阻塞任务完成拦截：如果 task 有未完成的依赖，阻止 status → done

📄 修改文件预期：`TaskModal.jsx` · `TaskCard.jsx` · `Board.jsx`

---

### 6.4 🔄 M4-P1 — 看板可视化（目标 71% → 100%）

**Day 6 前端（孔刚）：**
- [ ] Quick column filters：Topbar 或 Board 上添加 Todo/Doing/Done toggle 按钮
- [ ] Loading skeleton：`<Skeleton />` 可复用组件（卡片骨架 + 侧边栏骨架）
- [ ] Skeleton 显示在 Board loading 状态 + Sidebar stats loading 状态

📄 新文件预期：`Skeleton.jsx`
📄 修改文件预期：`Board.jsx` · `Column.jsx` · `Sidebar.jsx` · `Topbar.jsx`

---

### 6.5 🔄 M7 — 系统管理（目标 22% → 55%）

**Day 5 后端（周奕龙）：**
- [ ] `system_logs` 表（id uuid PK, user_id FK→profiles, action text, target_type text, target_id uuid, metadata jsonb, created_at timestamptz）
- [ ] RPC: `logActivity(action text, target_type text, target_id uuid, metadata jsonb)`
- [ ] 前端调用 logActivity 的关键位置：任务创建/编辑/删除、项目创建/删除、角色变更

**Day 6 后端（周奕龙）：**
- [ ] RPC: `exportAllData()` → JSON blob 包含 projects+tasks+labels+members
- [ ] RPC: `getFilteredTasks(status, priority, assignee, date_from, date_to, labels[])` → 为 M4-P4 和 M6 准备

📄 修改文件预期：`AdminModal.jsx`（集成 log viewer）
📄 RPC 迁移：新 migration SQL

---

## 七、存在问题与风险（第二周）

### 🔴 本周关注

| 问题 | 影响 | 缓解措施 |
|:----|:-----|:---------|
| 6 天交付 23 个新功能项 | 节奏紧凑，可能 slip | AI 辅助：OpenCode MCP 加速 SQL migration + 前端组件生成 |
| `task_dependencies` 循环依赖风险 | 数据完整性问题 | Trigger 校验 + RPC 内置检查 |
| 前后端并行开发 | 接口不匹配 | Day 2 前统一字段名/naming convention |
| test coverage 可能落后 | QA day 3-5 依赖代码就绪 | 前后端每完成一个模块立刻通知 QA |

### 🟡 已知限制

| 问题 | 说明 |
|:----|:-----|
| 通知系统 | 本周不实现，依赖后续 `notifications` 表 + trigger |
| 大数量性能（>100 tasks） | 加载骨架解决 UX 问题，虚拟滚动后续优化 |
| 跨浏览器测试 | 本周仅 Chrome dev；Firefox/Safari 下周 |

### 🟢 低优先级技术债务（不阻塞）

- 未使用组件清理（`InviteDialog.jsx`, `AccountModal.jsx`, `ProfileSettings.jsx`）
- `backend/` 目录清理
- CI/CD 配置

---

## 八、下周预告（6月29日 → 7月4日）

若本周达成 68% 目标，下周计划：

| 模块 | 目标 |
|:-----|:-----|
| **M4-P4 — List View** | 0% → 90% — 表格视图 + 筛选侧边栏 + 排序 + 批量编辑 |
| **M6 — Data Export** | 0% → 100% — PDF (jsPDF) + XLSX 导出 |
| **M4-P3 — 3D Sphere** ⭐ | 0% → 80% — Three.js 粒子球体可视化 |
| **M4-P2 — Gantt Chart** | 0% → 85% — frappe-gantt 集成 + 依赖箭头 |
| **M3-P3 — Priority/Labels** | 81% → 100% — 提醒/通知基础 |
| **M7 — System Admin** | 55% → 80% — Log viewer UI + backup restore |
| **Final — Deploy** | 20% → 90% — Vercel 部署 + 环境变量 |
| **Total** | 68% → **~88%** |

---

> **备注**：本文档为第二周开工计划与预测。各模块详情待开发完成后由负责人补充实际实现细节和代码引用。进度将在 Day 6 周中审计时更新。
>
> 📎 相关资源：[📄 需求规格](task.md) · [📊 追溯矩阵](requirements-matrix.md) · [📘 架构](architecture.md) · [🗄️ 数据库](database.md) · [📡 API](api.md) · [📦 功能总览](CHANGES.md) · [⚙️ Supabase README](../supabase/README.md)
