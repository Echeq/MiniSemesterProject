## Objective
Allow both admins and members to create, edit, and delete tasks.

## Files
- `frontend/src/components/Board.jsx` — remove isAdmin gates on onAddTask/onTaskClick
- `frontend/src/components/TaskModal.jsx` — remove isAdmin gate on submit/delete
- `supabase/migrations/20260630000006_allow_member_crud.sql` — new migration to broaden member RLS policies on tasks

## Steps
1. Write RLS migration: drop restrictive member update policy, add member insert/update/delete policies
2. Remove isAdmin gates from Board.jsx (onAddTask, onTaskClick)
3. Remove isAdmin gates from TaskModal.jsx (submit button, delete button, read-only message)
