## Objective
Filter task assignee dropdown to show only project members when a project is selected.

## Files
- frontend/src/components/TaskModal.jsx — add project member fetching + filter assignee list

## Steps
1. Add projectMembers state + fetch when projectId changes
2. Filter assignee dropdown by projectMembers when a project is selected
3. Keep showing all members for shared board (no project)
