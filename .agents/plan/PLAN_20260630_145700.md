## Objective
Make drag-and-drop work for all users on desktop; mobile uses tap-menu only (no drag).

## Files
- `frontend/src/components/Board.jsx`

## Steps
1. `handleDragStart` — remove isAdmin gate
2. `handleDragEnd` — remove isAdmin gate
3. `handleTaskTap` — remove isAdmin gate (all users can tap for mobile menu)
4. `canDrag` on both Columns — remove (default true, mobile disabled by isMobile in TaskCard)
5. Mobile menu move buttons — remove isAdmin gate
