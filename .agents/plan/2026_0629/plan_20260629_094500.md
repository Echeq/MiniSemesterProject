---
task: "Fix ProfileModal buttons stuck on 'Updating…'"
tier: SIMPLE
created: 2026-06-29T09:45:00
status: completed
inputs: { files_read: ["frontend/src/components/ProfileModal.jsx", "frontend/src/locales/en.json"] }
outputs: { files_written: ["frontend/src/components/ProfileModal.jsx"] }
---

## Objective

Fix email and password buttons in ProfileModal settings tab showing "Updating…" permanently instead of toggling between idle/updating labels.

## Result summary

Two bugs found and fixed in ProfileModal.jsx:

1. Email button (line 360) always rendered `t('profile.updating')` — never showed idle label.
2. Password button (line 370) same issue.
3. deleteAccount() had `setBusy(false)` only in `catch` — added `finally` block for cleanup.
