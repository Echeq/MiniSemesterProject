---
task: "Fix email change blocked by Supabase GoTrue validation of current email"
tier: COMPLEX
created: 2026-06-29T11:00:00
status: completed
inputs: {}
outputs: {}
---

## Objective

User's current auth email is `wwww@gmial.com` (typo domain). Calling `updateUser({ email: newEmail })` fails because GoTrue validates the CURRENT email and rejects `gmial.com` as invalid. Fix by creating a SECURITY DEFINER RPC that bypasses GoTrue validation and updating the frontend to use it.

## Steps

1. Create RPC `force_change_email` via Supabase MCP that directly updates `auth.users.email`
2. Update frontend `ProfileModal.jsx` to call the RPC when standard `updateUser` fails
3. Build and verify
