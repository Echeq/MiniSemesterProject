## Objective
Fix admin role being unable to create/edit tasks within projects, and members unexpectedly able to click-edit on desktop.

## Files
- `supabase/migrations/` (RLS policies — especially member task update policy)
- `frontend/src/hooks/useBoard.js` (task CRUD logic)
- `frontend/src/components/TaskCard.jsx` (click-to-edit entry point)
- `frontend/src/components/TaskModal.jsx` (edit form access control)
- `frontend/src/components/Board.jsx` (drag/drop + column interactions)

## Steps
1. Scout: Read all recent RLS migrations (especially `20260630000003_fix_member_task_update_policy.sql` and `20260630000004_admin_bypass_all_tables.sql`) and the frontend components for task editing.
2. Scout: Identify which RLS policy blocks admin writes in project scope, and where the frontend conditionally disables editing.
3. Deep: Fix the RLS policy (or frontend condition) so admins can always create/edit tasks in any project, and members are properly restricted.
4. Verify the fix is consistent across both backend (RLS) and frontend (UI gates).
