## Objective
Generate comprehensive presentation content (business processes, features, tech architecture) with diagrams, saved to `docs/presentation-content/`.

## Files
- `docs/presentation-content/` — target directory (exists, empty)
- `AGENTS.md` — architecture reference
- `frontend/src/` — component & hook analysis
- `supabase/migrations/` — schema & RPC analysis
- `supabase/README.md` — DB schema reference
- `frontend/package.json` — dependency/stack verification

## Steps
1. Scout explores codebase to gather all business/feature/tech details
2. Deep creates comprehensive .md with embedded Mermaid diagrams
3. If mermaid-cli available, render diagrams to PNG; otherwise keep Mermaid source
4. Write final document to `docs/presentation-content/presentation-overview.md`
5. Update `.agents/memory/index.md`
